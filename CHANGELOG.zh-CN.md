# 更新日志

[English](CHANGELOG.md) | **简体中文**

这里记录 Nightscout for Cloudflare 自身的更新。Nightscout 上游版本另行标注。
已有用户请先查看[升级说明](docs/DEPLOYMENT.zh-CN.md#更新已有部署)，按最初的安装方式操作。

## 2026-09-06 — 减少 AAPS 同步时的重复读取

**发布状态：已合入 GitHub `main`，尚未单独发布新的版本号。** 项目版本标识仍为
`1.1.1-beta`；仅凭这个版本号不能判断已有实例是否包含本次修复。对应
[读取优化提交](https://github.com/sid-luo/nightscout-for-cloudflare/commit/df9a894a0c7263fff0cb86a779f3cadf38f1b0a5)。

### 改了什么

- 减少 AAPS／NSClientV3 上传设备状态时，为更新网页而反复查询相同历史数据的开销。
- 血糖数据没有变化时复用查询结果；设备状态上传时合入本次变化。数据变化、重连后的
  实例重新启动或无法安全复用时，仍从数据库读取正确结果。
- 读取额度耗尽时，相关接口返回明确的临时不可用响应和重试等待时间。

本次没有删除历史记录，没有取消上传确认、历史同步或必要的网页更新，也没有修改
Nightscout 的计算逻辑。原有数据筛选、排序与输出范围继续保留。

### 验证结果

在同一组本地合成数据测试中，网页已连接时的一次设备状态上传，数据库读取从
**2,060 行降至 41 行，减少约 98%**；首次打开网页仍正常加载历史数据。
完整核心回归 **875 项测试通过**，类型检查和构建检查通过。

这是特定上传场景的本地结果，不代表整个账号全天读取量一定减少 98%，也不会提高
Cloudflare 的免费读写额度。[详细测量与适用边界](docs/performance/realtime-read-amplification.md)。

### 已有用户怎样取得更新

- **通过 GitHub 上的 Deploy to Cloudflare 按钮安装：** 包含项目构建更新器且符合自动
  更新条件的部署，可按[升级说明](docs/DEPLOYMENT.zh-CN.md#更新已有部署)重新构建。
- **通过网页快速安装器安装：** 当前尚无升级已有实例的网页入口。GitHub 更新不会自动
  更新你的实例；重新安装会创建另一个实例，不是原地升级。
- **本地命令行部署：** 按部署文档的本地更新流程更新现有实例。

本次 GitHub 推送没有自动更新网页安装器内置的发布包或任何用户现有部署。
